{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "PTPN Debug(LLDB)",
            "type": "lldb",
            "request": "launch",
            "program": "${workspaceFolder}/build/PTPN",
            "args": [
                "--cpus",
                "6",
                "--cores",
                "1",
                "--file",
                "${workspaceFolder}/example/label.dot",
                "--scg_type",
                "priority"
            ],
            "cwd": "${workspaceFolder}"
        },
    ]
}